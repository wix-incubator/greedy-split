{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Mocha",
      "program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
      "args": ["{test,src}/**/*.spec.js"],
      "cwd": "${workspaceRoot}"
    }
  ]
}